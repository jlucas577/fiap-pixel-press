import { Module } from '@nestjs/common';

/**
 * Fora do escopo do MVP (atividade-2). Estrutura declarada conforme standards.md;
 * controllers/services serão implementados numa entrega futura.
 */
@Module({})
export class ListasModule {}
